OPENROUTER_API_KEY=your_key_here

# Set to 1 in a public deployment (e.g. AWS) to hide the expensive frontier
# models from the picker and refuse them at the API, so visitors can only run
# the cheap budget-tier models. Leave unset in local dev to see all models.
PLOVERAI_BUDGET_ONLY=
